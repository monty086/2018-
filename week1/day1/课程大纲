

git
变量提升
特殊情况：
1. 带var和不带var
    带var就是一个变量，和不带var 相当于给全局对象window添加了一个属性
    如果你在代码中遇到字符，优先找变量，如果没有声明这个变量在window上找有没有这个属性
2. 连等的情况下
    只有第一个带var是一个变量，其他相当于给window添加一个属性
3. =号右边（带var）
    =号右边不进行变量提升，=左边才会进行变量提升
4. 函数中的return
    return右边的不进行变量提升，return下面的会进行变量提升
5. 函数为参数
    函数作为参数的时候不进行变量，
    自运行函数也不进行变量提升

